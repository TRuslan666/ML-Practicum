# ВАЖНО: PyTorch с поддержкой CUDA нужно ставить ОТДЕЛЬНО под вашу видеокарту.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   Проверка GPU:  python -c "import torch; print(torch.cuda.is_available())"

torch>=2.2          # ставится отдельно с CUDA 
torchvision>=0.17   # ставится отдельно с CUDA 

ultralytics>=8.2   
albumentations>=1.4
opencv-python>=4.9
pillow>=10.0
torchmetrics>=1.3  
pycocotools>=2.0.7  
scikit-learn>=1.4  
numpy>=1.26
pandas>=2.2
matplotlib>=3.8
seaborn>=0.13
tqdm>=4.66
pyyaml>=6.0
tensorboard>=2.16
jupyter>=1.0
ipykernel>=6.29
